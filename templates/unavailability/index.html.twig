{% extends 'layout.html.twig' %}

{% block page_title %}Historique{% endblock %}

{% block content %}

    <div class="jumbotron col-md-9 mx-auto mt-4">

        {% if is_granted('ROLE_ADMIN') %}
            <h1>Historique de l'occupation des salles</h1>
        {% else %}
            <h1>Vos réunions</h1>
        {% endif %}
        {% if unavailabilities is not empty %}
        <table class="table">
            <thead>
                <tr>
                    <th>Salle</th>
                    {% if is_granted('ROLE_ADMIN') %}
                        <th>Organisateur</th>
                    {% endif %}
                    <th>Début</th>
                    <th>Fin</th>
                    <th>Objet</th>
                    {% if is_granted('ROLE_ADMIN') %}
                        <th>Type</th>
                    {% endif %}
                    <th> </th>
                </tr>
            </thead>
            <tbody>
            {% for unavailability in unavailabilities %}
                <tr>
                    <td><a href="{{ path('room_show', {'id': unavailability.room.id}) }}">{{ unavailability.room.name }}</a></td>
                    {% if is_granted('ROLE_ADMIN') %}
                        <td>{{ unavailability.organiser.firstName ~ ' ' ~ unavailability.organiser.lastName }}</td>
                    {% endif %}
                    <td>Le {{ unavailability.startDate ? unavailability.startDate|date('d/m/Y à H:i') : '' }}</td>
                    <td>Le {{ unavailability.endDate ? unavailability.endDate|date('d/m/Y à H:i') : '' }}</td>
                    <td>{{ unavailability.object }}</td>
                    {% if is_granted('ROLE_ADMIN') %}
                        <td>{{ unavailability.type == 0 ? 'Réunion' : 'Autre' }}</td>
                    {% endif %}
                    <td>
                        <a href="{{ path('unavailability_show', {'id': unavailability.id}) }}"><i class="far fa-eye"></i></a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
        {% else %}
            <p class="lead">Vous n'avez encore organisé aucune réunion.</p>
        {% endif %}
    </div>

{% endblock %}
